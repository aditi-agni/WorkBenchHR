[build]
  command = "npm run build"
  publish = "dist"

# SPA: serve index.html for client-side routes (refresh /settings/profile, etc.)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
